import React from "react";
import {Authenticated} from "@pankod/refine-core";

export const Analysis = () => {
    return (
        <Authenticated>
            selam naber
        </Authenticated>
    );
};