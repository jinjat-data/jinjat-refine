import React from "react";
import {Authenticated} from "@pankod/refine-core";

export const Dashboard = () => {
    return (
        <Authenticated>
            selam naber
        </Authenticated>
    );
};